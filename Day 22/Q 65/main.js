// Q 65: Determine the Remainder: Make a function that gets two numbers and shows the leftover from 
// dividing them using the % sign. For example, remainder(5, 2) should give 1.
function modulus(number1, number2) {
    return number1 % number2;
}
;
console.log(modulus(5, 2));
