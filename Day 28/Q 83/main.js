//Converting to Upper and Lower Case: Create a function that takes a string, converts it to uppercase, 
//then to lowercase, and logs both.
function Case(name) {
    console.log("".concat(name.toLowerCase()));
    console.log("".concat(name.toUpperCase()));
}
Case("Areeba");
